# Compiled enumerator. Built by the gcc line in README.md; platform
# specific, so it is never committed. A tracked binary was caught in this
# project's own review once and removed.
experiments/E-002-kl-volkov-window-calibration/tree_counts
experiments/E-004-volkov-cutoff-bias/volkov_count

# Regenerated by check_mean_vs_annealed.py on its first run, tens of MB.
experiments/*/data/mc_*.txt

# Per-root scratch written by within_root_spread.py and cyc_vs_cycq.py.
experiments/*/data/tmp_*.txt

__pycache__/
*.pyc
*.o
