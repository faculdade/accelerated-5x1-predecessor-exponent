# build artifacts. `tree_counts_fixed` estava RASTREADO ate a rodada 13:
# um binario compilado de 26 KB, especifico da plataforma, com nome
# sugerindo ser "a versao consertada", indo para um deposito com DOI ao
# lado do fonte. Um revisor que clonasse nao saberia qual produziu os
# dados. Removido do rastreamento; o README diz como compilar.
tree_counts
tree_counts_fixed
# bulk intermediates regenerated by the scripts (megabytes, no information
# beyond what the committed result files and the READMEs already record)
data/mc_*.txt
data/q5_iid_meancheck.txt
data/tmp_*.txt
